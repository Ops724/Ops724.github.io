<section class="content-section">
  <header class="section-heading">
    <h1><%= page.title %></h1>
    <p><%= __('ui.tags_intro') %></p>
  </header>
  <div class="taxonomy-list taxonomy-tags">
    <% const currentLang = page_lang(page); %>
    <% site.tags.sort('name').forEach(function(tag) { %>
      <% const count = taxonomy_count_by_lang(tag, currentLang); %>
      <% if (count > 0) { %>
        <a class="taxonomy-item" href="<%- localized_taxonomy_url(tag.path, currentLang) %>">
          <span>#<%= tag.name %></span>
          <span><%= count %></span>
        </a>
      <% } %>
    <% }) %>
  </div>
  <div class="article-body">
    <%- page.content %>
  </div>
</section>
